# Jenkins Configuration as Code (CasC)
# This file is mounted into the controller at /var/jenkins_config/jenkins-casc.yaml
# and loaded by the JCasC plugin.
jenkins:
  systemMessage: "Jenkins managed by CasC"
  numExecutors: 0
  mode: NORMAL
  scmCheckoutRetryCount: 2

unclassified:
  location:
    adminAddress: "admin@example.com"
    url: "http://jenkins.pedersen.i/"
